; Transport headroom for one image per request. Application validation remains
; authoritative and enforces the exact 5 MiB per-image limit.
file_uploads = On
upload_max_filesize = 6M
post_max_size = 7M
